**Output/** — the **latest generated HTML** for this send (un-versioned, e.g. `<CODE>-YYYY-<id>.html`), kept in sync with the newest `Draft/` version. Used for browser/Klaviyo preview, QA and stakeholder review. Presence here is **not** sending approval — approval-to-send is tracked in `Brief/` + `Review/`, and the send is gated by CLAUDE.md §8.1. There is no top-level Output folder.
